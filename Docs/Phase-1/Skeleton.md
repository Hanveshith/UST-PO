
### Project Setup Instructions
    - npm init
    - npm i express @sap/cds @sap/cds-dk @sap-js/sqlite dotenv
    - mkdir db
    - mkdir srv
    - mkdir app 

### Git Management
    - git init
    - git status
    - git add --all
    - git status

    * add .gitignore file to exclude node_modules and environment files 